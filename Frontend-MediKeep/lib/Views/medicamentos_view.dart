import 'package:flutter/material.dart';

class MedicamentoView extends StatelessWidget {
  const MedicamentoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Medicamento View'),
      ),
    );
  }
}
