const usuario ={
    nombre: String,
    edad: Number,
    fechaNacimiento: Date,
    enfermedade: [String], //lo que estamos diciendo es que ese campo no guarda un solo valor, sino una lista (array) de valores
    contraseña: String
}